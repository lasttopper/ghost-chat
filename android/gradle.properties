org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# No AndroidX libraries are used (framework WebView only), but AGP 8.x is
# happiest with this on; it is harmless with zero dependencies.
android.useAndroidX=true
android.nonTransitiveRClass=true
