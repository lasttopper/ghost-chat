// Root build file. The Android Gradle Plugin version is pinned here; the app
// module applies it. No AndroidX / no external libraries are used, so the
// build only needs the Android SDK (no dependency resolution surprises).
plugins {
    id 'com.android.application' version '8.2.2' apply false
}
