<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform Material theme (no AppCompat dependency). Matches the app's
         dark chrome so there's no white flash while the PWA loads. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">#16181d</item>
        <item name="android:statusBarColor">#16181d</item>
        <item name="android:navigationBarColor">#000000</item>
    </style>
</resources>
