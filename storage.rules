rules_version = '2';

// Ghost Chat does not use Firebase Storage. Default-DENY so that if Storage is
// ever enabled on this project, no object can be read or written until rules
// are added here on purpose.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
